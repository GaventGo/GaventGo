// UI-only category type used for the static category tiles on the landing
// page (lib/data.ts). Real event data types live in types/database.ts.
export type EventCategory =
  | "Music"
  | "Comedy"
  | "Sports"
  | "Conferences"
  | "Parties";
